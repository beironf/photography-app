apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: photo-api
  annotations:
    kubernetes.io/ingress.global-static-ip-name: static-photo-api-ip
spec:
  rules:
  - host: photo.photography.beiron.se
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: photo-api
            port:
              number: 80
