package backend.photo.entities.settings

case class CameraSettings(focalLength: String,
                          fNumber: String,
                          iso: String,
                          exposureTime: String)
